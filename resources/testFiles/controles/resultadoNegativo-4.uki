## Suma con variable ##
NEGATIVO {
    uint A;
    A := 1UI;
    print(A + -2.); ## No hay error. Por convención, al hacer la conversion a entero, se toma el valor absoluto del flotante. El programa imprime '3'.##
}