MAIN {
    uint F6(uint X) {
        uint A;
        if (X > 3UI) 
            A := 5UI;
        else 
            if (X == 2UI)
                A := 3UI;
            else
                A := 1UI;
            endif;
        endif;
        return (A);
    }
}