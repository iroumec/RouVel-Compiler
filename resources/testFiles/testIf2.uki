MAIN {
    uint A(uint X) {
        if (X == 0UI) {
            if (X == 1UI){
                return(1UI);
            } endif;
        } else if (X == 1UI){
            return(1UI);
        } else {
            return (X);
        } endif; endif;
        return(0UI);
    }
}