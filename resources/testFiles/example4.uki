PROGRAMA {

    uint sds J;

    uint uint J;
    uint while uint if uint K;
    uint do L;
    LUIS uint H;
    TNIU uint uint UINT;

    uint U := := 55UI;
    uint V := uint X;
    uint W := ;

    while (5UI > 1UI) do {
        print(U);
    }

}