PROGRAMA {

    uint sds J;

    uint uint J;
    uint while uint if uint K;
    uint PALABRALARGUISIMARELARGALARGARELARGA do L;
    LUIS uint H;
    TNIU uint uint UINT;

    uint U := := 55UI;
    uint V := uint X;
    uint W := ;

    while (5UI > 1UI) do {
        print(U);
    }

    uint X =                                     4UI                                                                                               + 1.0F+1





;

}