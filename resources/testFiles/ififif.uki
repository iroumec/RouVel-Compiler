MAIN {
    uint F6(uint X) {
        uint A;
        if (X > 3UI) 
            A := 5UI;
        else 
            if (X == 2UI)
                A := 3UI;
            else
                if (X == 1UI)
                    A := 2UI;
                else 
                    if (X == 0UI)
                        if (X >= 0UI)
                            A := 54UI;
                        else {
                            if (X == 5UI)
                                A := 11UI;
                            endif;
                            A := X;
                        }
                        endif;
                    else 
                        A := 100UI;
                    endif;
                endif;
            endif;
        endif;
        return (A);
    }
}