MAIN {
    uint F1(cvr uint X, uint Y, cvr uint Z, uint A) {
        return (0UI);
    }

    uint A;
    uint B;
    uint C;
    uint D;

    A, B, C, D = 10UI, 20UI, 30UI, 40UI;
    F1(A->X, B->Y, C->Z, D->A);
}