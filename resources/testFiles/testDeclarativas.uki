DECLARATIVAS {
    ## ----------------------------------| Errores en declaración de variables |---------------------------------- ##
    uint A;                                     ## Válido ##
    uint B, C, D;                               ## Válido ##
    uint A                                      ## Falta de punto y coma ##
    uint B, C, D                                ## Falta de punto y coma ##
    uint E := 5UI;                              ## Declaración y asignación a la vez (inváldio) ##
    uint uint;                                  ## Declaración inválida ##

    ## ----------------------------------| Errores en declaración de funciones |---------------------------------- ##
    uint F1 (cvr uint X)                        ## Válido ##
    {
        X := X + 1.;
        return (X * 2.);
    };

    uint F2 (cvr uint X)                        ## Sin punto y coma, válido ##
    {
        X := X + 1.;
        return (X * 2.);
    }

    uint (uint X) {                             ## Función sin nombre ##
        return (X+1.);
    };

    uint F3 (uint X) {};                        ## Cuerpo de función sin sentencias ##

    uint F1 () {                                ## Función sin parámetros ##
        return (0.);
    };

    uint F2 (uint X,  ) {                       ## Parámetro vacío ##
        return (0.);
    };

    uint F3 (uint) {                            ## Nombre de parámetro formal faltante ##
        return (0.);
    };

    uint F4 (X) {                               ## Tipo del parámetro formal faltante ##
        return (0.);
    };

    uint F1 (do uint X) {                       ## Semántica de PP inválida ##
        return (0.);
    };

}