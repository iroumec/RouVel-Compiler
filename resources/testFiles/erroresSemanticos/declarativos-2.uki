## Este programa no ejecutará por errores semánticos declarativos ##
DECLARATIVOS {
    uint X;
    uint X(uint F) { ## Redeclaración de identificador, con variable y funcion ##
        return (F);
    };
}