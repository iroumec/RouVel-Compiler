## Este programa no ejecutará por errores semánticos declarativos ##
DECLARATIVOS {
    uint X;
    uint Y(uint F) { ## Redeclaración de identificador, con variable y parámetro ##
        uint F;
        F := 0UI;
        return (F);
    };
}