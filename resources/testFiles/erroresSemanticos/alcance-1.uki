ALCANCE {
    uint F (uint X) {
        uint A (uint Y) {
            uint B;
            return (Y*B);
        }
        A.B := 5UI;             ## El ambito de A no está al alcance ##
        return (A(2UI->Y));
    }

    print(F(2UI->X));
}