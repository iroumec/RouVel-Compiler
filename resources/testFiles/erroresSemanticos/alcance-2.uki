## Este programa no ejecutará por errores semánticos de alcance ##
ALCANCE {
    uint F (uint X) {
        uint A (uint Y) {
            uint B;
            return (Y*B);
        }
        C.B := 1UI;             ## El ambito C no existe ##
        return (A(2UI->Y));
    }

    print(F(2UI->X));
}