MAIN {
    uint F1(uint X) {
        if (X < 10UI) {
            if (X < 5UI) {
                return (5UI);
            }else{
                return (10UI);
            } endif;
        } endif;

        return(3UI);
    }
}