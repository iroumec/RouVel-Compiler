MAIN {
    uint F1 (uint X) {
        uint A;
        if (X >= 0UI)
            A := 54UI;
        else {
            if (X == 5UI)
                A := 11UI;
            endif;
            A := 51UI;
        } 
        endif;
        return (A);
    }
}