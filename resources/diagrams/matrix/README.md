# Ejecución

```sh
# Creación del ambiente.
python3 -m venv venv

# Activación del ambiente.
source venv/bin/activate

# Con el ambiente ya instalado:
pip install matplotlib

# Por último, con el ambiente activado:
python -u <archivo.py>
```
